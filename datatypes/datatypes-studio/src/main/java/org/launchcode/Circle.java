package org.launchcode;

public class Circle {
    public static Double getArea(Double radius){ //is this how you declare a method? public static + the output data type?
        return 3.14 * radius * radius; // can just return this since it's being grabbed from elsewhere
    }
}
